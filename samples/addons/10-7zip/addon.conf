# 7-Zip — automation-friendly silent MSI install (build time only).
# Download the x64 MSI from https://www.7-zip.org/download.html and drop it in
# this directory, then adjust `file=` to match the exact filename you fetched.
type=msi
file=7z2501-x64.msi
timeout=300
codes=0,3010
required=0
