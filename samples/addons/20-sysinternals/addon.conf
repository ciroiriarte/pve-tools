# Sysinternals — "copy files around" install (build time only).
# Extract the Sysinternals Suite into ./payload/ (see README.md). Everything
# under payload/ is copied to `dest`, and `dest` is appended to the system PATH.
type=copy
dest=C:\Tools\Sysinternals
payload=payload
path_add=1
required=0
